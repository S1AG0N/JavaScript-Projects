// Display an alert message when the page loads
window.alert("Welcome to my JavaScript project!");

// Declare two string variables
var Sent1 = "This is the beginning of the string";
var Sent2 = " and this is the end of the string";

// Concatenate the two strings and display the result on the page
document.write(Sent1 + Sent2);

// Write an arithmetic expression to the console
console.log("The result of 10 + 5 is: " + (10 + 5));
